{
  "id": "seller-assistant",
  "name": "Seller Assistant",
  "description": "Generic seller platform runtime with profile discovery, official-doc search, and policy-gated JavaScript execution.",
  "version": "2026.3.20",
  "skills": ["./skills"],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "currency": {
        "type": "string",
        "description": "Display currency fallback used when result formatting needs a currency code."
      },
      "locale": {
        "type": "string",
        "description": "Display locale used for formatted tool output."
      },
      "timeZone": {
        "type": "string",
        "description": "Fallback timezone used for display formatting."
      },
      "defaultProfile": {
        "type": "string",
        "description": "Profile to use when a tool call omits profileId."
      },
      "profiles": {
        "type": "array",
        "description": "Configured provider profiles that tools can search and execute against.",
        "items": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "id": {
              "type": "string",
              "description": "Stable local identifier for the profile."
            },
            "name": {
              "type": "string",
              "description": "Display name for the profile."
            },
            "provider": {
              "type": "string",
              "description": "Built-in provider name such as shopify."
            },
            "connection": {
              "type": "object",
              "description": "Provider-specific connection settings."
            },
            "policy": {
              "type": "object",
              "additionalProperties": false,
              "description": "Local authorization policy for this profile. Omit it to keep the profile read-only by default.",
              "properties": {
                "resources": {
                  "type": "object",
                  "description": "Allowed actions by business resource. Keys and action names may use * as a wildcard segment, for example {\"*\": [\"read\"], \"product\": [\"write\"]}.",
                  "additionalProperties": {
                    "type": "array",
                    "items": {
                      "type": "string"
                    }
                  }
                }
              }
            }
          },
          "required": ["id", "name", "provider", "connection"]
        }
      }
    }
  }
}
